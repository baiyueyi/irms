# Rule 01｜单智能体模式

- 默认执行智能体只有 `solr code`。
- 不做多 Agent 角色扮演，不输出 PM/Arch/BE/FE/QA 多角色对话。
- 已安装 skills 只作为辅助工具，不作为独立身份。
- 先由 `solr code` 统一理解需求、统一规划、统一实现、统一验证、统一收口。
- 提示词只写本次业务目标与验收点，不重复写长期工程规则。
- 需求不清晰时，只提阻塞实现的关键问题；非阻塞问题允许带明确假设继续推进。
